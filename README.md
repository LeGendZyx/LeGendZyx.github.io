# LeGendZyx.github.io

庄以煊的个人网站，基于 React、Vite 和 Tailwind CSS 构建，支持中英文切换（左上角按钮）。

在线地址：<https://legendzyx.github.io/>

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

推送到 `main` 后，GitHub Actions 会构建并部署到 GitHub Pages。
